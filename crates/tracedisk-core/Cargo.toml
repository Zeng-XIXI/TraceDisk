[package]
name = "tracedisk-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Read-only recovery engine for TraceDisk"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
